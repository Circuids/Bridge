namespace Circuids.Bridge;

/// <summary>
/// Identifies the operating system / platform.
/// </summary>
public enum PlatformIdentity
{
    Unknown,
    Android,
    IOS,
    Windows,
    Mac,
    Linux,
}
